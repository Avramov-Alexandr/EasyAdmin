<form method="get" action="{{ path('admin_history_search') }}" class="mb-3">
    <input type="text" name="q" placeholder="Search history..." value="{{ app.request.query.get('q', '') }}">
    <button type="submit">Search</button>
</form>