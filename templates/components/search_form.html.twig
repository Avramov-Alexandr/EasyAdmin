<form method="get" action="{{ path('admin_email_search') }}">
    <input type="text" name="q" placeholder="Search emails..." value="{{ app.request.query.get('q', '') }}">
    <button type="submit">Search</button>
</form>
