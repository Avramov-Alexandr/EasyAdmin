<form method="get" action="{{ path('admin_message_search') }}" class="mb-3">
    <input type="text" name="q" placeholder="Search messages..." value="{{ app.request.query.get('q', '') }}">
    <button type="submit">Search</button>
</form>