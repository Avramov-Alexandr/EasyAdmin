{% extends 'base.html.twig' %}

{% block body %}
    <h1>Search Results for "{{ query }}" in "{{ entity }}"</h1>

    {% if results is empty %}
        <p>No results found.</p>
    {% else %}
        <ul>
            {% for result in results %}
                <li>{{ result.name }} — {{ result.description }}</li>
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}
