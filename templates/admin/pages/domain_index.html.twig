{% extends '@EasyAdmin/crud/index.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
{% endblock %}

{% block content %}
    {{ parent() }}

    <!-- Обёртка для строки поиска -->
    <div class="d-flex flex-column align-items-start mt-3">
        <div class="w-100 d-flex justify-content-between align-items-center">
            <h5 class="mb-3">Results</h5>
        </div>
        <div class="search-container w-100">
            <form method="get" action="{{ path('admin', { crudAction: 'searc', crudControllerFqcn: 'App\\Controller\\Admin\\DomainCrudController' }) }}">
                <div class="input-group">
                    <input type="text" name="q" class="form-control" placeholder="Search domains..." value="{{ app.request.query.get('q') }}">
                    <button class="btn btn-primary" type="submit">Search</button>
                </div>
            </form>
        </div>
    </div>
{% endblock %}